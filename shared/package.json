{
  "name": "@ar-menu/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types, DTOs, and enums used by both backend and frontend.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
